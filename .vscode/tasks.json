{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "process excel file",
            "type": "shell",
            "command": "python ${workspaceFolder}/excelParser.py"
        },
        {
            "label": "start python server",
            "type": "shell",
            "command": "python ${workspaceFolder}/server.py"
        }
    ]
}